fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
pydantic>=2.7,<3
numpy>=1.26,<3
Pillow>=10,<13
# One OpenCV distribution only: mixing opencv-python / -headless / -contrib in one
# environment makes them overwrite each other's cv2 package.
opencv-contrib-python>=4.10
pytesseract>=0.3.13,<1
rapidfuzz>=3.9,<4
python-multipart>=0.0.9,<1
scikit-learn>=1.4
joblib>=1.3
pytest>=8,<9
# Optional: only needed to retrain the neural MRZ reader (python -m training.train_mrz_reader).
# Install a CUDA build from https://download.pytorch.org/whl/ to use a GPU.
# torch>=2.4
